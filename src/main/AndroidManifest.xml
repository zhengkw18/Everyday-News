<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.java.zhengkw">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".epidemicExperts.EpidemicExpertsActivity" />
        <activity android:name=".epidemicExpertsDetail.EpidemicExpertsDetailActivity" />
        <activity android:name=".epidemicEvents.EpidemicEventsActivity" />
        <activity android:name=".epidemicGraph.EpidemicGraphActivity" />
        <activity android:name=".epidemicGraphDetail.EpidemicGraphDetailActivity" />
        <activity android:name=".epidemicData.EpidemicDataActivity" />
        <activity android:name=".history.HistoryActivity" />
        <activity android:name=".newsList.NewsListActivity" />
        <activity android:name=".category.CategoryActivity" />
        <activity android:name=".newsPage.NewsPageActivity" />
        <activity android:name=".search.SearchActivity" />
    </application>

</manifest>